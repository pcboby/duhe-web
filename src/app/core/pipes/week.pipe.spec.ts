/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { WeekPipe } from './week.pipe';

describe('Pipe: Weeke', () => {
  it('create an instance', () => {
    let pipe = new WeekPipe();
    expect(pipe).toBeTruthy();
  });
});
