// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  productName: '堵河流域水库群防洪联合调度管控系统',
  productVersion: 'Development 1.0',
  productCopyright: 'All Copyright© by cjwsjy.',
  productArkWebBaseUrl: '/assets/ArkWeb', // arkweb资源文件读取路径
  // productPostListenerType: 'ArkWeb', // 接收外部PostMessage信息关建字木
  serviceRoot: ''
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
